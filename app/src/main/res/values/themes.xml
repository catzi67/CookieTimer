<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.CookieTimer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <!-- Setting primaryColor to blue_600 to influence default Material button tints -->
        <item name="colorPrimary">@color/blue_600</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <!-- Setting secondaryColor to gray_700 -->
        <item name="colorSecondary">@color/gray_700</item>
        <item name="colorSecondaryVariant">@color/gray_800</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/transparent_gray_800</item>
        <!-- Customize your theme here. -->

        <!-- Accent color, can influence default button text/tint in some older contexts or Material default. -->
        <!-- Keep colorAccent as blue_600 as a fallback -->
        <item name="colorAccent">@color/blue_600</item>

        <!-- Style for dialog buttons. These should now be picked up reliably by MaterialAlertDialogBuilder. -->
        <item name="buttonBarPositiveButtonStyle">@style/Widget.App.Button.AlertDialog.Positive</item>
        <item name="buttonBarNegativeButtonStyle">@style/Widget.App.Button.AlertDialog.Negative</item>
    </style>

    <!-- Custom style for the positive button in AlertDialogs -->
    <style name="Widget.App.Button.AlertDialog.Positive" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/blue_600</item>
    </style>

    <!-- Custom style for the negative button in AlertDialogs -->
    <style name="Widget.App.Button.AlertDialog.Negative" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/gray_700</item>
    </style>
</resources>